---
name: psf
description: "PSF 계기판. 세계의 현재 상태를 관측하고 축까지의 거리를 측정한다. 트리거: 'PSF 갱신', 'PSF 상태', '계기판', '국면', '브리핑', '주간 리뷰', '분기 진단', '시스템 상태', '자율 점검'. state.json이 3일 이상 stale이면 갱신 권고. 자율 3조건(기억+루프+울타리) 내장. GUARDRAILS.md 준수."
---

# PSF 실행 프로토콜

> 과정은 고정이 아니다. 데이터가 과정을 결정한다.
> 이 문서는 "반드시 해야 하는 것"과 "원칙"만 정의한다.

---

## 반드시 해야 하는 것 (불변)

```
1. errors.md 셀프검증 체크리스트를 실행 전에 확인한다.
2. P1~P4는 독립적으로 수집한다. 통합 검색으로 퉁치지 않는다.
3. PSF는 MCP를 직접 호출하지 않는다. Macro가 유일한 정량 수집자.
   (단, P층 정성 데이터와 축 신호 탐색에서는 Tavily 등을 직접 사용한다.)
4. 관측과 해석을 분리한다. PSF는 "무엇이 변했는가"를 말한다.
   "왜 변했는가"는 CE/TE의 일.
5. state.json에 기록한다. history/에 스냅샷을 남긴다.
6. 감속·훼손 신호를 가속 신호보다 먼저 점검한다.
```

---

## Phase 0: Macro 신선도 확인 + 자동 트리거

```
★ PSF 실행의 최초 단계. 다른 모든 단계보다 먼저 실행한다.

경로: C:\Users\이미영\Downloads\에이전트\macro\indicators\latest.json
참조: macro/SKILL-macro-indicators.md

1. latest.json의 "date" 필드를 읽는다.
2. 오늘 날짜와 비교하여 신선도를 판정한다.

판정 + 행동:
  fresh(~3일): 그대로 사용. Phase 1로 진행.
  stale(3~7일): ⚠️ 자동 트리거 실행.
    → macro 디렉토리(C:\Users\이미영\Downloads\에이전트\macro\)로 이동
    → PLUGIN-weekly-macro.md의 Step 1~10 수집 절차를 실행
    → latest.json 갱신 완료 확인
    → psf-monitor 디렉토리로 복귀, 갱신된 latest.json으로 Phase 1 진행
  expired(7일+): 🔴 자동 트리거 실행 (동일 절차).
    → 보고서에 "[macro 자동갱신 — 이전 N일 경과]" 태그 부착.

자동 트리거 규칙:
  수집자는 여전히 macro다. PSF가 macro의 수집 절차를 "대신 실행"하는 것.
  PSF가 독자적 MCP 수집 경로를 만들지 않는다.
  macro의 PLUGIN-weekly-macro.md + RULES.md + SKILL-macro-indicators.md를 그대로 따른다.
  갱신 결과는 macro/indicators/latest.json에 저장한다 (macro의 정본).
  PSF state.json에 "macro_auto_triggered": true, "trigger_reason": "stale|expired" 기록.

실패 시:
  MCP 접근 실패 등으로 수집 불가 → stale 데이터 그대로 사용.
  보고서에 "[macro 자동갱신 실패 — N일 전 데이터 사용]" 태그.
  사용자에게 "수동 /macro-weekly 실행 권고" 안내.
```

---

## 데이터 수집

### 정량 데이터 — Macro에서 읽기

```
경로: C:\Users\이미영\Downloads\에이전트\macro\indicators\latest.json
참조: macro/SKILL-macro-indicators.md

Phase 0에서 신선도 확인 + 자동 트리거 완료 후 이 단계에 도달한다.
이 시점에서 latest.json은 fresh 상태여야 한다.

PSF → Macro 매핑: collection.md 참조
★ PSF는 Macro의 데이터를 읽기만 한다. 직접 수집하지 않는다.
  (Phase 0의 자동 트리거는 macro의 수집 절차를 대행하는 것이지,
   PSF 고유의 수집 경로가 아니다.)
```

### 정성 데이터 — P층 독립 수집

```
원칙:
  각 Property(P1~P4)별로 맥락 기반 탐색을 실행한다.
  쿼리는 고정이 아니다. 직전 관측(state.json)에서 파생한다.

쿼리 생성 방법:
  1. state.json에서 해당 Property의 마지막 상태를 확인한다.
  2. "이 상태에서 다음에 일어날 수 있는 것은?"을 질문으로 만든다.
  3. "이 Property에서 놓치기 쉬운 것은?"도 별도 검색한다.

예시 (고정 쿼리가 아님, 상황에 따라 달라짐):
  P3이 🔴(호르무즈 48시간 최후통첩) → "Trump Iran Hormuz deadline result"
  P2가 ⚫(뉴스 부재) → "tariff trade policy changes this week" (묻힌 변화 확인)
  P4가 🟡(SEC 가이던스 시행) → "crypto regulation SEC enforcement after guidance"
  P1이 🟡(인하 0회 전망) → "Fed rate outlook inflation PCE"

"특이사항 없음"도 반드시 명시한다. 검색 없이 생략 금지.
```

### 축 신호 탐색 — 맥락 기반 MCP 동원

```
원칙:
  모든 MCP를 매번 쓰지 않는다.
  "오늘의 데이터에서 답이 필요한 질문"이 소스를 결정한다.

사용 가능한 소스 카탈로그: axis-map.md §데이터 소스 카탈로그 참조

예시:
  ④블록체인 관련 신호가 크면 → DeFiLlama TVL, CoinMetrics NUPL 추가 동원
  ⑨레짐 관련 신호가 크면 → FRED 추가 시리즈(국채 입찰 등) 확인
  변화가 없으면 → 기본 소스(FRED+CoinGecko)로 충분
```

---

## 관찰과 판정

### axis-map.md 프로토콜 실행

```
axis-map.md의 5단계를 실행한다.
모든 단계를 매일 깊이 할 필요 없다 — 변화의 크기가 깊이를 결정한다.

1단계: 변화 감지 — 뭐가 달라졌나?
2단계: 인과 추적 — 어디서 왔고, 어디로 가나?
3단계: 축 관련성 — 메가트렌드에 영향을 주는가?
4단계: 이상 탐지 — 모르는 것이 있는가?
5단계: 위험 거리 — 가장 가까운 위험은?
```

### P층 판정

```
원본 v3의 P1~P4 하위 속성(a/b/c)을 참조하여 각 Property를 판정한다.
판정 기준(🟢/🟡/🔴/⚫)은 원본 v3에 정의되어 있다.
합산 규칙도 원본 v3 참조.

★ 그러나 임계값은 절대 기준이 아니라 참고선이다.
  맥락에 따라 같은 수치도 다르게 판정할 수 있다.
  판정의 근거를 반드시 서술한다.
```

---

## 기록

```
1. state.json 갱신
   모든 Property 상태 + 수치 + 변경 사항 + alerts

2. history/ 스냅샷
   history/YYYY-MM-DD.json

3. 경보 (자동)
   Link 상태 변경, 국면 전환, Divergence, 임계 접근

4. PM_핵심예측 (LOOP 1 연동) ★ 개선
   보고서 발행 시 검증 가능한 예측을 추출하여 Notion에 기록.
   모호한 서술 금지. 수치 범위 + 기한 필수.
   금지: "축 건재" "국면 유지" (범위 없음)
   필수: "[기한]까지 [지표] [범위]. 이탈 시 오판."
   예: "3/30까지 HY OAS 350bp 미돌파. 돌파 시 L7 재판정."

5. 누적 갱신 (accumulation)
   매일 state.json 저장 시 accumulation.weekly도 함께 갱신.
   observations[]에서 축 관련 발견을 카운트하여 signal_counts에 반영.
   unclassified[]의 해소/지속 상태를 갱신.
   questions_open을 갱신.

   주간 리셋 시(W Loop):
     accumulation.weekly → history/YYYY-W##-summary.json 저장.
     weekly 필드 초기화.
   월간 리셋 시(M Loop):
     accumulation.monthly → history/YYYY-MM-summary.json 저장.
     monthly 필드 초기화.
```

---

## 출력 — 보고서(핵심) + JSON(상세) 분리

```
★ 원칙: 보고서는 핵심만. 상세는 전부 state.json.
  보고서 = 사람이 읽는 것. 20줄 이내.
  JSON = 기계가 읽고, 필요할 때 사람이 열어보는 것. 제한 없음.

보고서 형식 (고정):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[날짜] | macro [🟢🟡🔴](점수) | PSF [🟢🟡🔴] | [축 상태] | [한 단어 키워드]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[전문가]
1. [전파+돈] PSF 전파 위치 + macro 레짐 + 핵심 제약/교란. (1줄)
2. [방향] 축 상태 + 비가역 변화 + 레짐 방향. (1줄)
3. [위험] 가장 가까운 분기점 + 유동성 + 특이 신호. (1줄)

[해석]
1. (2~3문장. 개인투자자 수준. 전문 용어를 일상어로.)
2. (2~3문장.)
3. (2~3문장.)

[감시]
  날짜 | 이벤트 | 심각도
  (변화가 있는 것만. 3~5건.)

[예측 — 검증일]
  (수치 범위 + 기한. 2~4건.)

상세 → state.json
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

해석 작성 규칙:
  "BEI +29bp" → "물가 기대가 급등하기 시작했다"
  "S2 부동" → "기업 부도 위험은 아직 반응 안 함"
  "RRP $0.8B 고갈" → "시장에 새 돈을 공급하던 장치가 바닥남"
  전문 용어는 일상어로 바꾸거나 괄호 풀이.
  해석은 관측의 번역이지 인과 추론이 아니다.

보고서에 포함하지 않는 것 (전부 state.json으로):
  전파 지도, Layer별 수치, Link 전수 상태, Divergence 상세,
  발견 상세(cause/path/source), 미분류 상세(hypotheses),
  누적 데이터, 전수 점검 과정, 6단계 실행 과정.

출력 파일:
  1. 대화창 — 위 텍스트 형식으로 출력 (즉시 확인)
  2. reports/YYYY-MM-DD-briefing.html — 아래 HTML 템플릿으로 저장 (브라우저 열람)
  3. state.json — 상세 전부 (JSON)
  4. history/YYYY-MM-DD.json — 일일 스냅샷 (JSON)

HTML 보고서 템플릿:
  다크 테마. 모바일 대응. 720px max-width.
  구조:
    상태 바  — 색상 배지 (macro/PSF/축/키워드)
    전문가   — 3줄 (금색 번호)
    해석     — 3단락 (파란색 왼쪽 바, 회색 텍스트)
    감시     — 테이블 (critical=빨강, high=노랑, medium=회색)
    예측     — 칩 형태 (둥근 테두리)
    푸터     — "상세 → state.json"

  배지 색상:
    🟢 = #003d00 배경 + #4caf50 텍스트
    🟡 = #4a3800 배경 + #ffd600 텍스트
    🔴 = #3d0000 배경 + #ff5252 텍스트
    키워드 = #2a1a3e 배경 + #ce93d8 텍스트

  참조 파일: reports/2026-03-23-briefing.html (최초 템플릿)
```

---

## 통합 실행 원칙 ★ 개선 3: 축+PSF를 한 번에

```
축 모니터링과 PSF 관측을 따로 실행하지 않는다.
하나의 실행에서 두 렌즈를 모두 적용한다.

실행 흐름 (단일):
  1. 데이터 수집 (1회)
  2. axis-map.md 1~5단계 (변화 감지 → 인과 추적 → 축+PSF 동시 판단)
     → 2단계(인과 추적)에서 P→S→F 전파를 추적하면 PSF 관측이 된다.
     → 3단계(축 관련성)에서 메가트렌드 영향을 판단하면 축 모니터링이 된다.
     → 이 둘은 같은 데이터의 다른 단면이다. 분리 실행은 낭비.
  3. 6단계 전수 점검 (1회)
  4. state.json 저장 (1회)
  5. 보고서 출력 (1건 — 핵심 3줄로 시작)

보고서에서:
  PSF 렌즈 = "전파가 어디까지 왔는가" (핵심 3줄의 1줄)
  축 렌즈 = "방향이 바뀌고 있는가" (핵심 3줄의 2줄)
  두 렌즈가 하나의 서술 안에 자연스럽게 공존한다.
```

---

## 모니터링 루프 트리거

```
상세: monitoring.md 참조

일일: "브리핑", "오늘 시장", "PSF 갱신", "축 모니터링" ← 모두 같은 실행
주간: "주간 리뷰", "축 리뷰"
월간: "월간 점검", "배관 점검" + 투영 확률 조정
분기: "분기 진단", "축 진단" + 투영 전면 재구성
긴급: "긴급 점검" + [사유]
수시: "KC 상태", "질문 상태"
투영: "12개월 투영", "시나리오", "전망"
```

---

## 투영 엔진

```
상세: projection-engine.md 참조

역할: 3시스템 관측을 입력으로, 12개월 시나리오를 산출.

실행 주기:
  분기 (Q Loop): 전면 재구성. 6개 구성 요소 모두. projection.json 리셋.
  월간 (M Loop): 확률 재조정. 분기 조건 충족 여부 점검.
  일일: 분기 조건 충족 시에만 보고서에 "투영 변화" 1줄 추가.
  긴급: 시나리오 분기 이벤트 시 즉시 재조정.

산출물:
  projection.json — 시나리오 + 사이클 + 축 속도 + 자산 함의
  reports/YYYY-QN-projection.html — 분기 투영 보고서

일일 보고서와의 연결:
  일일 보고서의 [감시] 항목이 투영의 분기 조건과 연동.
  분기 조건 충족 시 → "투영 변화: Bear 25%→35%" 1줄 추가.
```

---

## media-monitor 연결

```
상세: ../media-monitor/interfaces-psf.md 참조

media-monitor = 독립 모듈. 미디어 내러티브 + 센티먼트 분석.
psf-monitor와 합치지 않는다. 산출물만 공유.

일일 보고서에 media 센티먼트 추가 (media 실행 시에만):
  [감시] 아래에:
  [센티먼트] media: [판정] | 포화도: [수준] | 재귀: [상태] | 미반영: [있으면]

자동 트리거 조건 (psf-monitor → media-monitor Quick 실행):
  PSF 국면 전환 (🟢→🟡 / 🟡→🔴)
  축 훼손 신호(⚠️) 발생
  투영 시나리오 분기 조건 충족
```

---

## question-forge 연결

```
상세: ../question-forge/interfaces-psf.md 참조

question-forge = 독립 모듈. 8대 질문 생성 (글로벌 5 + 국내 3).
"무엇을 물어야 하는가"를 구조화.

주간 리뷰(W Loop) 시 연동:
  psf-monitor W Loop 결과 → question-forge 입력
  → 8대 질문 → psf-monitor next_questions 보강
  → 질문의 검증 앵커 → 다음 주 D Loop 탐색 방향

주간 보고서에 질문 추가 (question-forge 실행 시):
  [이번 주 질문]
  GQ1: [한 줄] | 앵커: [데이터 기준]
  KQ1: [한 줄] | 파이프: [전이 경로]
```

---

## 자율 운영 레이어

```
자율 3조건: 기억(Memory) + 루프(Loop) + 울타리(Guardrail)
상세: GUARDRAILS.md, SCHEMAS.md 참조
코드: core/validate.py, core/snapshot.py, core/autonomy.py

자율 수준: Level 3 (자율 실행)
  울타리(Green/Yellow/Red Zone) 안에서 관측·판정·기록을 무승인 실행.
  구조 수정(ontology/axis 개정)은 Yellow Zone → 승인 필요.
```

### 세션 시작 스캔 (모든 세션의 첫 동작)

```
1. state.json 로딩 → 현재 국면 확인
2. macro/latest.json 신선도 확인 (Phase 0)
3. next_questions에서 deadline 도래 항목 필터
4. errors.md 셀프검증 체크리스트 확인
5. 루프 유형 판별 (D/W/M/Q)

종합 알림 출력:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 시스템 상태 ({날짜})
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
국면: {🟢🟡🔴} | macro: {🟢🟡🔴}(점수) | 정합: {상태}
질문: {N}건 open ({deadline 도래 N건})
Link: {활성 Link 목록}
축: {변화 있는 축}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

알림 없으면 요약 1줄만. 과잉 알림 금지.
```

### 후처리 (관측 완료 후)

```
관측 완료 시 자동 실행:

1. core/validate.py 실행 → state.json 스키마 검증
   WARN 발견 시 보고서에 "[검증 경고: ...]" 태그

2. core/snapshot.py 실행 → history/YYYY-MM-DD.json 저장
   delta_vs_prev 자동 계산

3. core/autonomy.py scan 실행 → 이상 감지
   이상 발견 시 에스컬레이션 (GUARDRAILS.md 참조)

도구 체인:
  python core/validate.py          → 스키마 검증
  python core/snapshot.py          → 스냅샷 저장 + delta 계산
  python core/autonomy.py scan     → 이상 감지
  python core/autonomy.py summary  → 자율 행동 세션 요약
```

### 참조 파일 (자율 운영)

```
GUARDRAILS.md          ← Green/Yellow/Red Zone + 에스컬레이션
SCHEMAS.md             ← JSON 스키마 정의 (state, projection, history)
core/validate.py       ← state.json 스키마 검증
core/snapshot.py       ← 스냅샷 저장 + delta 계산
core/autonomy.py       ← 이상 감지 + 세션 요약
```

---

## 실패 패턴과 방어 (Step 6: FAIL)

> 7단계 프로토콜 Step 6. 정상 경로만으로는 80점. 방어 경로가 있어야 90점+.
> 상세 오류 이력: errors.md 참조. 여기는 F-테이블(구조적 방어).

### F-테이블 (실패 누적 — 삭제 금지, 추가만)

```
┌─────┬──────────────────────┬─────────────────────────┬────────────────────────┬───────────────────────┐
│ #    │ 실패 패턴             │ 원인                     │ 방어 규칙               │ 감지 방법              │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-01│ 유동성 3종 누락        │ 기억 의존, 온톨로지       │ collection.md vs       │ validate.py에서        │
│     │ (WALCL/RRP/TGA)      │ 교차 점검 없이 수집       │ ontology 1:1 대조       │ 자동 검출 (ERR-001)    │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-02│ S/F에 "방향" 누락      │ 절대값만 보고 변화율 무시 │ 모든 S/F에 direction    │ validate.py에서        │
│     │                      │                         │ (↑/→/↓) 필수           │ 자동 검출 (ERR-002)    │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-03│ Link 트리거에 시계열   │ Property 수집(현재)과    │ Link 점검 시 "현재"와   │ 셀프검증 체크리스트     │
│     │ 기간 누락              │ Link 체크(추세) 혼동     │ "시계열" 분리 확인      │ 항목 (ERR-003)         │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-04│ S2에서 IG OAS 누락     │ "HY가 대표" 선입견       │ 신용은 반드시 HY+IG    │ validate.py에서        │
│     │ (HY만 수집)           │                         │ 쌍으로 수집             │ 자동 검출 (ERR-004)    │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-05│ PMI Tavily 단독 수집   │ "FRED에 없다"로 대체     │ Tavily 의존 지표는      │ 셀프검증 체크리스트     │
│     │ (교차검증 없음)        │ 수용                    │ MCP 프록시 1건+ 필수    │ 항목 (ERR-005)         │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-06│ BEI(T5YIE) P1 또는    │ PSF 관점만 보고          │ BEI는 P-재정 + S1      │ 셀프검증 체크리스트     │
│     │ S1 한쪽에만 배치       │ 교차 배치 놓침          │ 양쪽에 반드시 존재      │ 항목 (ERR-006)         │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-07│ F1에서 DXY만 보고      │ "DXY가 대표" 선입견      │ F1은 DXY + 금 + TLT    │ 셀프검증 체크리스트     │
│     │ 금·TLT 누락           │                         │ 3중 점검 필수           │ 항목 (ERR-007)         │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-08│ 출처 태그 누락         │ 스키마에 source 없었음   │ 모든 observation에      │ validate.py에서        │
│     │ ([MCP]/[뉴스]/[추정]) │                         │ 출처 + 시점 필수        │ 자동 검출 (ERR-008)    │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-09│ MCP 비율 미계산        │ "채워졌으면 됐다" 사고   │ quality.mcp_ratio 필수  │ validate.py에서        │
│     │ (품질 메타 부재)       │                         │ <50%이면 품질 낮음 태그 │ 자동 검출 (ERR-009)    │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-10│ 판 자기강화 루프를     │ 판 존재 확인만 하고      │ 매 관측 시 각 판의      │ 셀프검증 체크리스트     │
│     │ 확인 안 함             │ 루프 활성 여부 미점검    │ loop_active 확인        │ 항목 (F-14 방지)       │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-11│ 가속 신호만 보고       │ 확인 편향. 좋은 소식에   │ "감속·훼손 신호를       │ R-04 인지 오류 방어.   │
│     │ 감속 신호 무시         │ 끌림                    │ 가속보다 먼저 점검"     │ axis-map 3단계에서     │
│     │                      │                         │ (불변 원칙 7)           │ 확인                   │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ F-12│ 관측을 해석처럼 서술   │ 관측/해석 경계 모호      │ "은유/의인화를 제거해도 │ Self-Audit Q3에서      │
│     │ ("위험하다", "좋다")   │                         │ 의미 전달되는가?" 테스트│ 확인                   │
├─────┼──────────────────────┼─────────────────────────┼────────────────────────┼───────────────────────┤
│ ... │ 운영하면서 추가        │                         │                        │                       │
└─────┴──────────────────────┴─────────────────────────┴────────────────────────┴───────────────────────┘
```

### 오류 유형 패턴 (12건 분석)

```
누락:   42% (F-01, F-04, F-06, F-07, F-10)
불완전: 33% (F-02, F-03, F-05, F-09)
경계:   25% (F-08, F-11, F-12)

핵심 대책:
  누락 → ontology ↔ collection 1:1 교차 검증 + validate.py 자동화
  불완전 → 셀프검증 체크리스트 + "방향"/"시계열" 등 구체 항목 강제
  경계 → R-01~R-08 인지 오류 방어 + Self-Audit 불편한 질문
```

---

## 진화 규칙 (Step 7: EVOLVE)

> 7단계 프로토콜 Step 7. 지침이 진화하면 LLM이 학습한 것과 같다.

### Self-Audit (매 관측 완료 후)

```
Q1. 전수 점검: P1~P5, S1~S5, F1~F5 전부 state.json에 있는가?
    → 하나라도 빠지면 ⚠. 빠진 이유를 "[미확인 — 사유]"로 기록.

Q2. 출처 정합성: 모든 observation에 [MCP:소스] 또는 [뉴스:출처] 태그가 있는가?
    → quality.mcp_ratio < 50%이면 ⚠.

Q3. 관측/해석 분리: 보고서에서 은유·의인화·가치판단 문장이 있는가?
    → "위험하다", "좋다", "~에 베팅", "~을 먹이고" 등 발견 시 ⚠.
    → 재서술: "VIX 26.78, 경계 구간" (O) / "위험하다" (X)

Q4. 감속 우선: 가속 신호를 먼저 서술하고 감속 신호를 나중에 서술했는가?
    → 감속을 먼저 점검했는가? (불변 원칙 7, F-11 방지)
    → 확인 편향 방어: "이 축이 건재하다"를 먼저 의심.

Q5. 미분류 보존: 설명 안 되는 신호를 버리지 않고 [미분류]로 기록했는가?
    → unclassified에 0건인데 "없었다"로 넘어갔으면 ⚠.
    → "없음"도 명시적으로 "미분류 0건 — 특이사항 없음" 기록.
```

### 이슈 적재 조건

```
Self-Audit에서 ⚠ 발견 시:

일회성 판별:
  "이번 관측만의 문제인가?" → 보고서 내 수정 후 끝.

시스템성 판별:
  동일 ⚠가 2회 이상 발생 → errors.md에 ERR-xxx 추가.
  MCP 수집 실패 반복 → collection.md 폴백 경로 보강.
  F-테이블 패턴과 일치 → 해당 F-xx에 발생 횟수 추가.
  신규 패턴 → F-테이블에 새 항목 추가.
```

### 갱신 주기

```
일일 (D Loop): Self-Audit Q1~Q5 실행. 오류 발견 시 errors.md 기록.
주간 (W Loop): errors.md 신규 오류 리뷰. F-테이블 패턴 확인. 방어 규칙 추가 여부 판단.
분기 (Q Loop): ontology.md / axis.md 개정 가능. 투영 전면 재구성.
              F-테이블 전수 리뷰. 해소된 패턴은 "[해소]" 태그 (삭제 안 함).
```

### 갱신 주체

```
자동 (LLM): Self-Audit 실행, 오류 기록, F-테이블 발생 횟수 갱신.
승인 필요: F-테이블 방어 규칙 추가/수정, SKILL.md 실행 순서 변경.
사용자: ontology/axis 개정, 불변 규칙 변경, GUARDRAILS.md 수정.
```

### 불변 규칙 (진화의 하한선)

```
어떤 갱신에서도 바꾸면 안 되는 것:

1. PSF 3층(판-구조-흐름) 구조 자체를 해체하지 않는다.
2. S1~S5, F1~F5 전수 수집을 "주요 N개만"으로 축소하지 않는다.
3. 관측과 해석의 분리를 훼손하지 않는다.
4. F-테이블을 삭제하지 않는다. 누적만 한다.
5. Self-Audit에서 "불편한 질문"(Q3, Q4)을 제거하지 않는다.
6. errors.md의 오류 기록을 삭제하지 않는다.
7. 원본-PSF 온톨로지 엔진.md의 최고 우선순위를 변경하지 않는다.
8. "데이터가 과정과 답을 결정한다"는 열린 구조 원칙을 폐기하지 않는다.
```

---

## 7단계 프로토콜 채점 (psf-monitor)

```
Step 1 WHY:    ✅ "세계의 현재 상태를 관측하고 축까지의 거리를 측정"
Step 2 WHAT:   ✅ IN(관측)/OUT(해석·예측·행동권고)/ADJACENT(CE·TE·macro)
Step 3 WHEN:   ✅ D/W/M/Q 트리거 + 긴급 + 수시 + 금지(해석·예측)
Step 4 HOW:    ✅ Phase 0 → 수집 → axis-map 6단계 → 기록 → 출력
Step 5 FORMAT: ✅ 텍스트 20줄 + HTML + state.json. 좋은/나쁜 예시(CLAUDE.md)
Step 6 FAIL:   ✅ F-01~F-12 테이블 + 오류 유형 패턴 분석
Step 7 EVOLVE: ✅ Self-Audit Q1~Q5 + 갱신 주기/주체 + 불변 규칙 8개

점수: 7/7 → 정밀도 90점+ (자기 진화 가능)
```
